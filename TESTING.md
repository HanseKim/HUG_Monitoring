# 온전 — 탭별 테스트 입력 가이드

`npm run dev` 실행 후 http://localhost:5173 접속. 모든 API는 MSW 목이며, 목 딜레이 0.8~1.5초 후 결과가 표시됩니다.
사이드바 하단 **기준 지역**을 바꾸면 임대차계약·HUG 심사·든든전세 폼의 지역 기본값이 함께 바뀝니다.

## 1. 임대차계약 (/tenant)

목 로직: 주택유형이 전세가율을 결정 → 전세가율이 위험도를 결정.
(아파트 70% / 연립 85% / 기타 88% / 다세대 90% / 오피스텔 95%, 오피스텔은 위험도 ×1.7)

| 시나리오 | 입력 | 기대 결과 |
|---|---|---|
| 안심 | 유형 **아파트**, 보증금 350,000,000 | 전세가율 70% → 사고확률 ~3.2%, 안심(초록) |
| 주의 | 유형 **연립주택**, 보증금 220,000,000 | 전세가율 85% → ~7%, 주의(주황), 감액인수 안내 |
| 위험 | 유형 **오피스텔**, 보증금 190,000,000 | 전세가율 95% → ~80.1%, 위험(빨강), HR0·HR3 발동, 전세피해지원센터 안내 |
| 선순위 가중 | 위 아무 입력 + 상세 정보에서 선순위권리 **근저당설정** | 위험도 ×1.35, HR1 발동 |
| 보험 미가입 | 상세 정보에서 보증보험 **미가입** | HR2 발동 |
| 에러 상태 | 상세주소에 `오류` 포함 (예: "오류 테스트 1-2") | 422 에러 → 인라인 메시지 + 다시 시도 버튼 |

보증금 금액 자체는 등급에 영향 없음(유형이 전세가율을 결정). 아무 상세주소나 입력해도 됩니다.

## 2. HUG 심사 (/underwrite)

목 로직: 전세가율 = 보증금/주택가액. EL = PD×LGD×보증금, 보험료 = 보증금×0.24%.
**승인 = EL ≤ 보험료 AND 전세가율 ≤ 90%**. 선순위금액이 크면 LGD가 커져 거절로 기움.

직접 입력 예시:

| 시나리오 | 보증금 | 주택가액 | 선순위 | 기대 판정 |
|---|---|---|---|---|
| 승인 | 350,000,000 | 500,000,000 | 0 | 전세가율 70% → 승인 |
| 거절(전세가율) | 280,000,000 | 290,000,000 | 0 | 96.6% → 거절 |
| 거절(선순위) | 250,000,000 | 300,000,000 | 150,000,000 | LGD 급등 → 거절 |

CSV 업로드: 저장소의 [samples/underwrite_sample.csv](samples/underwrite_sample.csv) (12건, 승인·거절 혼합)를 드롭존에 끌어다 놓고 → 미리보기 확인 → "12건 일괄 심사" → 요약 스탯 + 테이블, 행 클릭 시 우측 슬라이드오버 상세.
컬럼 검증 데모: CSV에서 `housePrice` 헤더를 지우고 올리면 누락 컬럼 경고가 표시됩니다.

## 3. 모니터링 (/monitor)

고정 시드 8건 — 입력 없이 바로 확인:
- 기본 화면: 변동 있는 5건 (등급하락 3 + 금리경보 2)
- 필터 **등급하락만**: C-2026-0042(T3 근저당), C-2026-0117(T1 시세하락), C-2026-0233(T4 지역리스크)
- 필터 **경보만**: T2 금리 2건 (등급 무변동)
- 검색으로만 보이는 무변동 계약: `잠실`, `유성구`, `해운대` 또는 `C-2026-0150` / `C-2026-0188` / `C-2026-0210` 검색
- 없는 검색어(예: `제주`) → 빈 상태 메시지

## 4. 든든전세 (/recovery)

목 로직: 다세대·연립 + 수도권(서울/인천/경기) → 자산화 기대값이 높아 **셀프낙찰** 우위.
오피스텔 → 배당률이 높아 **배당대기** 우위. 대항력임차인 "유"면 셀프낙찰 게이트 탈락.

직접 입력 예시 (지역은 사이드바 연동, 상세주소는 아무거나):

| 시나리오 | 입력 | 기대 경로 |
|---|---|---|
| 셀프낙찰 | 지역 서울, 유형 **다세대주택**, 대위변제액 280,000,000, 감정가 320,000,000, 최저매각가 224,000,000, 유찰 1, 명도 양호, 대항력 무 | 셀프낙찰 (게이트 3개 ✓) |
| 배당대기 | 지역 인천, 유형 **오피스텔**, 대위변제액 190,000,000, 선순위 140,000,000, 감정가 200,000,000, 최저매각가 98,000,000, 유찰 3, 명도 점유중, 대항력 **유** | 배당대기 (대항력 게이트 ✗) |

결과 레포트 우상단 **PDF로 저장** → 인쇄 미리보기에서 사이드바·입력폼이 숨겨지는지 확인.

CSV 업로드: [samples/recovery_sample.csv](samples/recovery_sample.csv) (8건) → 경로별 분포 도넛 + 테이블 → 행 클릭 시 하단에 단건 레포트.
