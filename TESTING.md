# 온전 — 테스트 가이드 (v2 리스크 관제 콘솔)

`npm run dev` → http://localhost:5173. 모든 API는 MSW 목(딜레이 0.8~1.5초).
사이드바(관제 레일) 하단 **기준 지역**은 심사·회수 폼의 기본 지역과 연동됩니다.

## / 플로우 개요
입력 불필요. 파이프라인 다이어그램(심사→모니터링→회수분기→데이터 축적)과 단계별 현황이
표시되고, 노드 클릭 시 해당 단계로 이동. 모니터링 노드의 "임차인 고지 3건 대기"는 시드
데이터의 등급하락 건수와 연동됩니다.

## /assess 심사·등급 (01)
승인 조건: EL ≤ 보험료 AND 전세가율 ≤ 90%. 판정과 함께 모델1 최초 등급(안심/주의/위험)이 표시됩니다.

| 시나리오 | 보증금 | 주택가액 | 선순위 | 기대 결과 |
|---|---|---|---|---|
| 승인 + 안심 | 350,000,000 | 500,000,000 | 0 | 전세가율 70% → 승인, 안심 |
| 거절(전세가율) | 280,000,000 | 290,000,000 | 0 | 96.6% → 거절, 위험 |
| 거절(선순위) | 250,000,000 | 300,000,000 | 150,000,000 | LGD 급등 → 거절 |

CSV: [samples/underwrite_sample.csv](samples/underwrite_sample.csv) 12건 → 일괄 심사 → 행 클릭 시 슬라이드오버.
`housePrice` 헤더 삭제 후 업로드하면 컬럼 검증 경고.

## /monitor 상시 모니터링 (02)
시드 8건. 기본 변동 5건(등급하락 3 + 금리경보 2), 무변동 3건은 `잠실`·`유성구`·`해운대` 검색으로만.
**등급하락 카드에는 [임차인 고지 발송] 버튼** — 클릭 시 발송됨 상태로 전환(데모).

## /policy 정책 인사이트 (03)
입력 불필요, 전부 목데이터 명시: 회수율 예측 vs 실제 추이(환류 효과), 사고 예상 세그먼트
테이블, 인수기준 제안 3건.

## /recovery 회수 전략 (04)
상단: 전략 수립 컨텍스트(가용 재원·든든전세 매입 여력·대기 물건 — 목데이터).

| 시나리오 | 입력 | 기대 경로 |
|---|---|---|
| 든든전세 | 서울 + 다세대, 대위변제 280,000,000 / 감정가 320,000,000 / 최저매각가 224,000,000 / 유찰 1 / 명도 양호 / 대항력 무 | 든든전세 (게이트 3 ✓) |
| 배당대기 | 인천 + 오피스텔, 대위변제 190,000,000 / 선순위 140,000,000 / 최저매각가 98,000,000 / 유찰 3 / 점유중 / 대항력 유 | 배당대기 (게이트 ✗) |

레포트 하단에 **환류 배너**(판정 결과 → 모델1 학습 데이터 축적) 표시.
[PDF로 저장] → 인쇄 미리보기에서 레일·입력폼 숨김 확인.
CSV: [samples/recovery_sample.csv](samples/recovery_sample.csv) 8건 → 도넛 + 테이블 → 행 클릭 시 단건 레포트.

## 참고
- 구 `/tenant`(임차인 셀프진단)는 새 플로우(HUG 내부 관점)에서 제외 — 경로는 /assess로 리다이렉트.
  `POST /api/tenant/score` 목 핸들러는 API 계약 유지를 위해 남아 있음.
